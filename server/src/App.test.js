/**
 * Basic test to ensure Jest configuration works
 */

describe('IKY Server API', () => {
  it('should have basic exports available', () => {
    // This is a simple smoke test to ensure the test runner works
    expect(true).toBe(true);
  });

  it('should handle basic math operations', () => {
    expect(1 + 1).toBe(2);
  });
});
